// tailwind.config.js

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // Add all HTML and TypeScript files in src to Tailwind content
  ],
  theme: {
    extend: {
      // You can extend the default theme here if needed
    },
  },
  plugins: [],
}
